import { defineConfig } from 'vite'

export default defineConfig({
  // Set base to '/' for custom domains, or '/repo-name/' for github.io
  // I'll leave it as '/' by default but add a comment for the user.
  base: './', 
})
